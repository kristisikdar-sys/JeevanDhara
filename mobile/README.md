# JeevanDhara Mobile (Flutter)

- Install Flutter SDK
- flutter pub get
- Run on Android emulator: `flutter run`
- Backend base URL used: `http://10.0.2.2:4000`
- Google Maps requires API key: replace KEY_PLACEHOLDER in AndroidManifest if needed