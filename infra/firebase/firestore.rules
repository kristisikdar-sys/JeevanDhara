rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /blood_requests/{docId} {
      allow read, write: if request.auth != null; // demo only
    }
    match /reminders/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}